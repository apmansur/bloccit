 <h1><%= @sponsered.title %></h1>
 
 <div class="row">
   <div class="col-md-8">
     <p><%= @sponsered.body %></p>
   </div>
   <small>This add is sponsered</small>
   <div class="col-md-4">
     <%= link_to "Edit", edit_topic_sponsered_path(@sponsered.topic, @sponsered), class: 'btn btn-success' %>
     <%= link_to "Delete Post", [@sponsered.topic, @sponsered], method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this post?' } %>
   </div>
 </div>